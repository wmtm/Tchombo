# Password for the /admin question-management page. Change this before deploying.
ADMIN_PASSWORD=tchombo-dodo

# Render sets PORT automatically in production; only needed if you want a custom port locally.
# PORT=3001
